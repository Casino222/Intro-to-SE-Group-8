version: 2.1
orbs:
  python: circleci/python@2

jobs:
  test-python:
    docker:
      - image: cimg/python:3.12-node
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "Pipfile.lock" }}
            - v1-dependencies-
      - python/install-packages:
          args: --dev
          pkg-manager: pipenv
      - save_cache:
          paths:
            - ".venv"  # Path where Pipenv installs packages
          key: v1-dependencies-{{ checksum "Pipfile.lock" }}
      - run:
          name: Run tests
          command: pipenv run python manage.py test

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
      - run:
          name: found github actions config
          command: ':'

workflows:
  build-and-test:
    jobs:
      - test-python
    # - deploy:
    #     requires:
    #       - test-python
