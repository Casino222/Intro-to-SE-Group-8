function display() {
    // Get the value from the input tag
    var userInput = document.getElementById("uname").value;

    // Use the retrieved value
    alert(userInput);
    // You can use this value in any way you want, such as displaying it on the webpage, processing it, etc.
}